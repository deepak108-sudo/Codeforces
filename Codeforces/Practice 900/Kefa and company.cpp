#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,d; cin>>n>>d;

    vector<pair<long long,long long>> vec;

    while(n--){
        int a,b;

        cin>>a;
        cin>>b;

        vec.first.push_back(a);
        vec.second.push_back(b);
    }

    
}