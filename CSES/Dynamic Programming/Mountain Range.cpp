#include<bits/stdc++.h>
using namespace std;

int n;
vector<int> a;
int solve(int i,int prev){

    int total=0;
    if(i==0){
        total=1+solve()
    }
    else{
        total=1+solve(i+1,)
    }
}

int main(){
    int n; cin>>n;
    a.resize(n);
    for(int i=0;i<n;i++){
        cin>>a[i];
    }

    solve(0,0);
}